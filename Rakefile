desc 'Setup with example files'
task :setup do
	# Copy Secrets
	 puts 'Copying JFISecret.m example into place...'

	 `cp Justaway/JFISecret.m.SAMPLE Justaway/JFISecret.m`

	 # Done
	 puts "Done! You\'re ready to get started!"
end

# Run setup by default
task :default => :setup